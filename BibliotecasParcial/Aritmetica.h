#ifndef FUNCIONES_H_INCLUDED
#define FUNCIONES_H_INCLUDED

/** \brief
 *  suma()
 * \param n1 float Guarda el operando A
 * \param n2 float Guarda el operando B
 * \return float Retorna la suma de los operandos
 *
 */
float suma(float n1, float n2);

/** \brief
 *  resta()
 * \param n1 float Guarda el operando A
 * \param n2 float Guarda el operando B
 * \return float Retorna la resta de los operandos
 *
 */
float resta(float n1, float n2);

/** \brief
 *  multiplicacion()
 * \param n1 float Guarda el operando A
 * \param n2 float Guarda el operando B
 * \return float Retorna el producto de los operandos
 *
 */
float multiplicacion(float n1, float n2);

/** \brief
 *  division()
 * \param n1 float Guarda el operando A
 * \param n2 float Guarda el operando B
 * \return float Retorna la division de los operandos
 *
 */
float division(float n1, float n2);

/** \brief
 *  factorial()
 * \param n1 int Guarda el operando A
 * \return int Retorna el factorial del operando A
 *
 */
int factorial(int n1);

#endif
